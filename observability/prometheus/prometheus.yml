global:
  scrape_interval: 15s       # Pull metrics every 15 seconds
  evaluation_interval: 15s

scrape_configs:
  - job_name: "iterallm_api"
    static_configs:
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics
    scrape_interval: 10s     # More frequent for the API